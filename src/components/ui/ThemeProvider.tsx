'use client';

/**
 * Re-export ThemeProvider from the canonical providers location.
 * This file exists for backward compatibility with imports from '@/components/ui/ThemeProvider'.
 */
export * from '@/components/providers/ThemeProvider';
